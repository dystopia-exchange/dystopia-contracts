comment:
  layout: "reach,diff,flags,files,footer"
  behavior: default
  require_changes: false
github_checks:
  comment: true
  annotations: true
coverage:
  status:
    patch:
      default:
        target: 97%
    project:
      default:
        threshold: 1%
ignore:
  - "contracts/test"
